First compute the center of all the object
{
    create_boundung_box()
    {
        take four values for every object
        [
            minx, miny,
            minx, maxy,
            maxx, miny,
            maxx, maxy,
        ]
        find min(all minx, all miny)
        find max(all maxx, all maxy)
    }
}

